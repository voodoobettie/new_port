<p id="notice"><%= notice %></p>

<h1>Blogs be here</h1>


<div>
  <div>
    <tr>
      <h1>Title</h1>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </div

  <div>
    <% @blogs.each do |blog| %>
      <div>
        <h3><%= blog.title %></h3>
        <div><%= blog.body %></div>
        <td><%= link_to 'View it', blog %></td>
        <td><%= link_to 'Fix it', edit_blog_path(blog) %></td>
        <td><%= link_to 'Kill it', blog, method: :delete, data: { confirm: 'This is madness. Are you sure?' } %></td>
      </dir>
    <% end %>
  </div>
</div>
<br>

<%= link_to 'New Blog', new_blog_path %>
