<h1>Portfolio items</h1>

<% @portfolio_items.each do |port| %>
  <h2><%= port.title %></h2>
  <h3><%= port.subtitle %></h3>
  <p><%= port.body %></p>
  <%= image_tag port.thumb_image unless port.thumb_image.nil? %>
  <%= link_to "Edit", edit_portfolio_path(port.id) %>
  <% end %>

<%= link_to "Add a portfolio item", new_portfolio_path %>
